import React from "react";

type Props = {
  title: string;
  icon: string;
  buttonContent: string;
  image: string;
  duration: string;
  description: string;
};
const Body1 = ({
  title,
  icon,
  buttonContent,
  image,
  duration,
  description,
}: Props) => {
  return <div></div>;
};
export default Body1;
